{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://lead-t1pm.onrender.com/api/$1" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
